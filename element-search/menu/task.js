const link = Array.from(document.querySelectorAll('.menu__link');

for (let i; i < link.length; i++) {
	link.onclick = function(){
		const parent = link.closest('.menu__link');
		oldParent = parent.
	}
}